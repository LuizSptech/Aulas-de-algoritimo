
public class Desafio01 : MonoBehaviour
{

    public int x = 5;
    public int y = 7;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int soma = x + y;
        Debug.Log("a soma de x+y e igual a " + soma );
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
